services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=local
      - CORS_ORIGINS=http://localhost:5173
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
